//Conditional rendering
// let name; 
// let newUser = true; 
// if (Math.random() > 0.5 && newUser) { 
// 	name = "Mike" 
// } else { 
// 	name = "Susan" 
// } 


//Conditional rendering with component:
// function LogInOutButton(props) {
//     const isLoggedIn = props.isLoggedIn;
//       if (isLoggedIn) {
//         return <LogoutButton />;
//       } else {
//       return <LoginButton />;
//     }

//the when the LogInLogOutButton parent component is used, the prop can be passed in
{/* <LogInOutButton isLoggedIn={false} /> */}

